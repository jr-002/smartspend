# Environment Variables Configuration
# This file serves as a template for required environment variables
# Copy relevant variables to your .env.local for development

# ===============================================
# CLIENT-SIDE VARIABLES (VITE_ prefix required)
# ===============================================
# These variables are bundled with the client and are publicly visible

# Supabase Configuration (Required)
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your_supabase_anon_key_here"

# Application Configuration
VITE_APP_VERSION="1.0.0"
NODE_ENV="development"

# Optional Client-side Services
VITE_SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"

# ===============================================
# SERVER-SIDE VARIABLES (Edge Functions Only)
# ===============================================
# These are managed through Supabase Secrets in production

# Groq AI Configuration (Server-side Edge Functions)
GROQ_API_KEY="your_groq_api_key_here"

# Supabase Server Configuration (For Edge Functions)
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your_supabase_anon_key_here"
SUPABASE_SERVICE_ROLE_KEY="your_service_role_key_here"

# ===============================================
# DEPLOYMENT NOTES
# ===============================================
# 
# For Vercel deployment:
# 1. Set VITE_* variables in Vercel Environment Variables dashboard
# 2. Server-side secrets are managed through Supabase Secrets
# 3. Do not include server-side keys in Vercel environment variables
#
# For local development:
# 1. Copy this file to .env.local
# 2. Fill in the actual values for your project
# 3. Server-side variables can be added to .env.local for testing
