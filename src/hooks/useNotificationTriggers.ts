// Component that handles notification triggers
// This is a utility hook that should be used in the main app component

export const useNotificationTriggers = () => {
  // This hook is currently disabled to prevent circular dependencies
  // It should be implemented directly in the main app component
  console.log('Notification triggers hook loaded');
};